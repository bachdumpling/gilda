{% liquid
  assign section_id = section.settings.custom_id | default: section.id
  assign section_class = 'section-' | append: section.type
%}

<section
  id="{{ section_id }}"
  class="{{ section_class }} bg-[#f2efed]"
>
  <div class="grid md:grid-cols-2 border-y border-black overflow-hidden">
    {%- comment -%} Left Column - Image {%- endcomment -%}
    <div class="order-2 md:order-1 overflow-hidden h-full">
      {% if section.settings.image != blank %}
        <img
          src="{{ section.settings.image | image_url: width: 2000 }}"
          srcset="
            {{ section.settings.image | image_url: width: 600 }} 600w,
            {{ section.settings.image | image_url: width: 1200 }} 1200w,
            {{ section.settings.image | image_url: width: 2000 }} 2000w
          "
          alt="{{ section.settings.image.alt | escape }}"
          loading="eager"
          width="100%"
          height="100%"
          class="w-full min-h-full object-cover"
        >
      {% endif %}
    </div>

    {%- comment -%} Right Column - Content {%- endcomment -%}
    <div class="order-1 md:order-2 flex flex-col items-center justify-center px-4 sm:px-4 md:px-9 lg:px-20 xl:px-30 2xl:px-[140px] py-10 sm:py-14 md:py-10 lg:py-20 xl:py-30 2xl:py-[100px] border-b md:border-b-0 md:border-l border-black">
      <div class="flex flex-col gap-10 w-full max-w-md">
        {%- comment -%} Contact Section {%- endcomment -%}
        <div class="flex flex-col gap-3">
          <h2 class="font-mono font-medium text-base text-black uppercase">
            {{ 'sections.contact.contact_heading' | t }}
          </h2>
          <div class="font-sans text-sm leading-relaxed text-[#333333]">
            <p>
              {%- if section.settings.contact_text != blank -%}
                {{ section.settings.contact_text }}
              {%- else -%}
                {{ 'sections.contact.contact_description' | t }}
              {%- endif -%}
              {%- if section.settings.contact_email != blank %}
                <a href="mailto:{{ section.settings.contact_email }}" class="underline decoration-solid">
                  {{- section.settings.contact_email -}}</a
                >.
              {%- endif -%}
            </p>
          </div>
        </div>

        {%- comment -%} Wholesale Inquiries Section {%- endcomment -%}
        <div class="flex flex-col gap-3">
          <h2 class="font-mono font-medium text-base text-black uppercase">
            {{ 'sections.contact.wholesale_heading' | t }}
          </h2>
          <div class="font-sans text-sm leading-relaxed text-[#333333]">
            <p>
              {%- if section.settings.wholesale_text != blank -%}
                {{ section.settings.wholesale_text }}
              {%- else -%}
                {{ 'sections.contact.wholesale_description' | t }}
              {%- endif -%}
              {%- if section.settings.wholesale_email != blank %}
                <a href="mailto:{{ section.settings.wholesale_email }}" class="underline decoration-solid">
                  {{- section.settings.wholesale_email -}}
                </a>
              {%- endif -%}
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "t:sections.contact.name",
  "tag": "section",
  "class": "section-contact",
  "settings": [
    {
      "type": "header",
      "content": "t:sections.contact.settings.image.header"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "t:sections.contact.settings.image.label",
      "info": "t:sections.contact.settings.image.info"
    },
    {
      "type": "header",
      "content": "t:sections.contact.settings.contact_section.header"
    },
    {
      "type": "html",
      "id": "contact_text",
      "label": "t:sections.contact.settings.contact_text.label",
      "info": "t:sections.contact.settings.contact_text.info"
    },
    {
      "type": "text",
      "id": "contact_email",
      "label": "t:sections.contact.settings.contact_email.label",
      "info": "t:sections.contact.settings.contact_email.info",
      "default": "info@gildaclothing.com"
    },
    {
      "type": "header",
      "content": "t:sections.contact.settings.wholesale_section.header"
    },
    {
      "type": "html",
      "id": "wholesale_text",
      "label": "t:sections.contact.settings.wholesale_text.label",
      "info": "t:sections.contact.settings.wholesale_text.info"
    },
    {
      "type": "text",
      "id": "wholesale_email",
      "label": "t:sections.contact.settings.wholesale_email.label",
      "info": "t:sections.contact.settings.wholesale_email.info",
      "default": "wholesale@gildaclothing.com"
    },
    {
      "type": "header",
      "content": "t:sections.contact.settings.section_settings.header"
    },
    {
      "type": "text",
      "id": "custom_id",
      "label": "t:sections.contact.settings.custom_id.label",
      "info": "t:sections.contact.settings.custom_id.info"
    }
  ],
  "presets": [
    {
      "name": "t:sections.contact.presets.contact.name",
      "settings": {
        "contact_email": "info@gildaclothing.com",
        "wholesale_email": "wholesale@gildaclothing.com"
      }
    }
  ]
}
{% endschema %}
